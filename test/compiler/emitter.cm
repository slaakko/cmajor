using System;

namespace Test.Compiler
{
    public unit_test void TestConjunctionDisjunction()
    {
        int pos = 4;
        int n = 10;
        if (!(pos >= 0 && pos < n))
        {
            #assert(false);
            return;
        }
        #assert(true);
    }
}
