using System;
using System.Collections;

//  Writes:
//  6

void main()
{
    List<int> ints;
    ints.Add(1);
    ints.Add(2);
    ints.Add(3);
    int init = 0;
    int sum = Accumulate(ints.CBegin(), ints.CEnd(), init, Plus<int>());
    Console.WriteLine(sum);
}
