using System;
using System.Collections;
using System.IO;

//  Writes:
//  0, 1, 2, 3

void main()
{
    List<int> list;
    list.Add(0);
    list.Add(2);
    list.Add(3);
    list.Add(3);
    CopyBackward(list.CBegin() + 1, list.CEnd() - 1, list.End());
    list[1] = 1;
    Console.Out() << list << endl();
}
