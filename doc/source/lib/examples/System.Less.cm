using System;
using System.Collections;

//  Writes:
//  1, 2, 3

void main()
{
    List<int> list;
    list.Add(3);
    list.Add(2);
    list.Add(1);
    Sort(list, Less<int>());
    Console.Out() << list << endl();
}
