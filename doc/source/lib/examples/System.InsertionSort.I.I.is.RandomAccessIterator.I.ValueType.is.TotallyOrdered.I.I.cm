using System;
using System.Collections;

//  Writes:
//  -1, 0, 4, 4, 6, 7

void main()
{
    List<int> list;
    list.Add(7);
    list.Add(-1);
    list.Add(4);
    list.Add(6);
    list.Add(0);
    list.Add(4);
    InsertionSort(list.Begin(), list.End());
    Console.Out() << list << endl();
}
